#pragma once
#include "al_structs.h"
///redefined some of the event names at EVENTS.h

//combine ALLEGRO_EVENT_TIMER with al_wait_for_event(event_queue, &ev) 
// when if (al_is_event_queue_empty(event_queue) is empty, return from functon queue







